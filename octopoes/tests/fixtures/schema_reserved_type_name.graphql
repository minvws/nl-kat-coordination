# Native Kat
interface BaseObject {
   primary_key: ID!
}

interface OOI implements BaseObject {
   primary_key: ID!
   human_readable: String!
}

type Test implements OOI & BaseObject {
   primary_key(natural_key: [String] = ["test"]): ID!
   human_readable: String!
   test: String!
}

type Query implements OOI & BaseObject {
    test: Test!
}
