{
    "title": "Arguments",
    "type": "object",
    "properties": {
        "PORTS": {
            "title": "PORTS",
            "maxLength": 2048,
            "type": "string",
            "description": "A valid Nmap ports argument (-p)."
        }
    },
    "required": [
        "PORTS"
    ]
}
