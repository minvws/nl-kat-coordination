{
  "title": "Arguments",
  "type": "object",
  "properties": {
    "REQUEST_TIMEOUT": {
      "title": "Timeout for slow lookup requests, defaults to 60 seconds",
      "type": "integer",
      "minimum": 5,
      "maximum": 1000,
      "default": 60
    }
  }
}
