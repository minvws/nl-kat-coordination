{
  "title": "Arguments",
  "type": "object",
  "properties": {
    "REPLY_FQDN": {
      "title": "REPLY_FQDN",
      "maxLength": 255,
      "type": "string",
      "description": "A trusted fully qualified domain name to catch callbacks, such as 'cve.stillekat.nl'."
    }
  },
  "required": [
    "REPLY_FQDN"
  ]
}
