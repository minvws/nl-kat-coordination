{
    "title": "Arguments",
    "type": "object",
    "properties": {
        "MESSAGE": {
            "title": "Input text to give to the boefje",
            "type": "string",
            "description": "Some text so the boefje has some information to work with. Normally you could feed this an API-key or a username"
        },
        "NUMBER": {
            "title": "Optional number",
            "type": "integer",
            "minimum": 1,
            "maximum": 9,
            "description": "A number between 1 and 9. To show how many emojis you want to add to the greeting"
        }
    },
    "required": [
        "MESSAGE"
    ]
}