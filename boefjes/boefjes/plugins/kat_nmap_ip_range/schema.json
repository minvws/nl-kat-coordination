{
    "title": "Arguments",
    "type": "object",
    "properties": {
        "TOP_PORTS_TCP": {
            "title": "TOP_PORTS_TCP",
            "type": "integer",
            "minimum": 1,
            "maximum": 65535,
            "description": "Scan TOP_PORTS_TCP most common TCP ports. Defaults to 250."
        },
        "TOP_PORTS_UDP": {
            "title": "TOP_PORTS_UDP",
          "type": "integer",
          "minimum": 1,
          "maximum": 65535,
            "description": "Scan TOP_PORTS_UDP most common UDP ports. Defaults to 10."
        }
    },
    "required": []
}
