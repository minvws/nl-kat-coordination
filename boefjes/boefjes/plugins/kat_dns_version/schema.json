{
  "title": "Arguments",
  "type": "object",
  "properties": {
    "TIMEOUT": {
      "title": "TIMEOUT",
      "type": "integer",
      "description": "Timeout for requests to the dns serverst",
      "default": 30,
      "minimum": 0
    }
  }
}
